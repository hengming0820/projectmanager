# 抽屉卡片边框显示修复

## 问题描述

用户反馈项目管理文档页面的"文章信息与历史"抽屉中，卡片样式显示不一致：

- ❌ 文章元信息卡片没有边框
- ❌ 编辑历史卡片没有边框
- ❌ 看起来与其他页面（会议记录、模型测试）不一致

## 根本原因

### 1. Element Plus 的 shadow="never" 属性

```vue
<el-card shadow="never" class="meta-card"></el-card>
```

当使用 `shadow="never"` 时，Element Plus 会移除卡片的：

- 默认阴影
- 默认边框
- 默认圆角

### 2. CSS 优先级问题

项目文档页面使用的是普通 CSS：

```vue
<style scoped>
```

而会议记录和模型测试页面使用 SCSS：

```vue
<style scoped lang="scss">
```

虽然我们在 CSS 中添加了边框：

```scss
.meta-card {
  border: 1px solid var(--el-border-color-lighter);
}
```

但由于 Element Plus 内部样式的优先级更高，这个边框被覆盖了。

## 解决方案

### 修复方法

为所有卡片的边框样式添加 `!important` 和必要的属性：

```scss
.meta-card {
  border: 1px solid var(--el-border-color-lighter) !important;
  border-radius: 8px;
  overflow: hidden;
}

.history-card {
  border: 1px solid var(--el-border-color-lighter) !important;
  border-radius: 8px;
  overflow: hidden;
}
```

### 修复内容

1. **添加 `!important`**：确保边框样式不被覆盖
2. **添加 `border-radius: 8px`**：恢复圆角效果
3. **添加 `overflow: hidden`**：确保内容不会超出圆角边界

## 已修复的文件

### 1. 项目文档页面 ✅

**文件**: `src/views/project/management/components/ArticleDetailView.vue`

```scss
/* 文章元信息区域 */
.article-meta-section {
  .meta-card {
    border: 1px solid var(--el-border-color-lighter) !important;
    border-radius: 8px;
    overflow: hidden;
  }
}

/* 编辑历史区域 */
.history-section {
  .history-card {
    border: 1px solid var(--el-border-color-lighter) !important;
    border-radius: 8px;
    overflow: hidden;
  }
}
```

### 2. 会议记录页面 ✅

**文件**: `src/views/project/articles/meeting/index.vue`

同样的修复已应用

### 3. 模型测试页面 ✅

**文件**: `src/views/project/articles/model-test/index.vue`

同样的修复已应用

## 视觉效果对比

### 修复前 ❌

```
┌─ 文章元信息 ────────────┐
│                        │  ← 没有边框！
│ 所属部门               │
│ 研发部标准注组          │
└────────────────────────┘

┌─ 编辑历史 ─────────────┐
│                        │  ← 没有边框！
│ 2025-10-17 16:08       │
└────────────────────────┘
```

### 修复后 ✅

```
┌────────────────────────┐
│ 📋 文章元信息          │  ← 有淡灰色背景
├────────────────────────┤  ← 有底部边框
│ 📁 所属部门            │
│ [研发部标准注组]       │
└────────────────────────┘  ← 有外边框和圆角

┌────────────────────────┐
│ 📜 编辑历史            │  ← 有淡灰色背景
├────────────────────────┤  ← 有底部边框
│ ⏰ 2025-10-17 16:08   │
│ 👤 系统管理员          │
└────────────────────────┘  ← 有外边框和圆角
```

## 完整的卡片样式

现在所有页面的卡片都有：

### 卡片外观

- ✅ **边框**: `1px solid` 淡灰色
- ✅ **圆角**: `8px`
- ✅ **阴影**: 无（`shadow="never"`）
- ✅ **溢出**: 隐藏（`overflow: hidden`）

### 卡片标题

- ✅ **背景**: 淡灰色 `var(--el-fill-color-light)`
- ✅ **文字**: 深灰色 `var(--art-text-gray-900)`
- ✅ **图标**: 主题色 `var(--el-color-primary)`
- ✅ **底部边框**: `1px solid` 淡灰色

### 卡片内容

- ✅ **padding**: `20px`
- ✅ **背景**: 白色（默认）

## 技术要点

### 为什么需要 !important

1. **Element Plus 内部样式优先级高**

   - Element Plus 的组件样式使用了较高的选择器优先级
   - `shadow="never"` 会应用内部样式移除边框

2. **scoped CSS 的限制**

   - 虽然使用了 `:deep()`，但不是所有样式都能穿透
   - 直接作用在 `<el-card>` 上的样式可能需要 `!important`

3. **跨文件一致性**
   - 使用 `!important` 确保在不同的 CSS 环境下（CSS vs SCSS）都能正常工作

### 为什么添加 overflow: hidden

```scss
.meta-card {
  border-radius: 8px;
  overflow: hidden; // 重要！
}
```

- 确保卡片内容（特别是 header）不会超出圆角边界
- 让圆角效果完整显示
- 防止内部元素的背景色延伸到圆角外

## 验证方法

### 1. 清除缓存并刷新

```
Windows: Ctrl + Shift + R
Mac: Cmd + Shift + R
```

### 2. 检查卡片边框

打开任意文章页面（会议记录、模型测试、项目文档），点击"文章信息"按钮：

应该看到：

- ✅ 两个卡片都有明显的灰色边框
- ✅ 卡片有8px的圆角
- ✅ 标题有淡灰色背景
- ✅ 标题有底部分隔线

### 3. 开发者工具检查

按 F12 打开开发者工具，选择卡片元素，应该看到：

```css
.meta-card {
  border: 1px solid #e4e7ed !important;
  border-radius: 8px;
  overflow: hidden;
}
```

## 相关文件

- `src/views/project/management/components/ArticleDetailView.vue`
- `src/views/project/articles/meeting/index.vue`
- `src/views/project/articles/model-test/index.vue`

## 总结

通过添加 `!important`、`border-radius` 和 `overflow: hidden`，成功修复了抽屉卡片边框不显示的问题。

现在所有页面的抽屉样式完全一致！✅

### 关键修改

```scss
// 修改前 ❌
.meta-card {
  border: 1px solid var(--el-border-color-lighter);
}

// 修改后 ✅
.meta-card {
  border: 1px solid var(--el-border-color-lighter) !important;
  border-radius: 8px;
  overflow: hidden;
}
```

🎉 **问题已解决！请硬刷新浏览器查看效果。**
