# 角色权限管理使用指南

## 🎯 功能说明

当前系统**已经实现了完整的角色权限管理功能**，可以通过界面配置角色的菜单访问权限。

**位置**：系统管理 > 角色管理

---

## 📋 功能清单

### ✅ 已实现的功能

1. **角色CRUD**

   - ✅ 创建角色
   - ✅ 编辑角色
   - ✅ 删除角色
   - ✅ 查询角色列表

2. **权限配置**

   - ✅ 菜单权限树展示
   - ✅ 勾选/取消权限
   - ✅ 全选/取消全选
   - ✅ 展开/收起树节点
   - ✅ 保存权限配置

3. **权限应用**
   - ✅ 用户登录后按角色过滤菜单
   - ✅ API权限验证
   - ✅ 实时生效（重新登录后）

---

## 🛠️ 使用步骤

### 1. **创建角色**

#### 步骤：

1. 进入 **系统管理 > 角色管理**
2. 点击右上角 **"新增角色"** 按钮
3. 填写角色信息：

| 字段       | 说明                      | 示例                     |
| ---------- | ------------------------- | ------------------------ |
| 角色名称   | 显示名称                  | `数据分析师`             |
| 角色编码   | 唯一标识符（英文+下划线） | `data_analyst`           |
| 描述       | 角色说明                  | `负责数据分析和报表制作` |
| 启用       | 是否启用该角色            | `开启`                   |
| 权限字符串 | 自动生成（只读）          | -                        |

4. 点击 **"提交"**

![创建角色](./images/create-role.png)

---

### 2. **配置权限**

#### 步骤：

1. 在角色列表中，点击某个角色的 **"更多"** 按钮（⋮）
2. 选择 **"菜单权限"**
3. 在弹出的权限树中勾选该角色需要的菜单

**权限树结构示例：**

```
☑ Dashboard（仪表板）
  ☑ Console（控制台）

☑ Project（项目管理）
  ☑ ProjectDashboard（项目仪表板）
  ☑ ProjectManagement（项目列表）

☑ Task（标注任务）
  ☐ TaskPool（任务池）
  ☐ MyWorkspace（我的工作台）
  ☐ TaskReview（任务审核）

☑ Performance（标注绩效）
  ☑ PersonalPerformance（个人绩效）
  ☐ TeamPerformance（团队绩效）
```

4. 点击 **"保存"**

![配置权限](./images/role-permission.png)

---

### 3. **给用户分配角色**

#### 步骤：

1. 进入 **系统管理 > 用户管理**
2. 点击某个用户的 **"编辑"** 按钮
3. 在 **"角色"** 下拉框中选择角色
4. 点击 **"保存"**

![分配角色](./images/assign-role.png)

---

### 4. **验证权限**

用户重新登录后，系统会：

- ✅ 自动过滤菜单（只显示有权限的菜单）
- ✅ 隐藏无权限的页面
- ✅ 阻止无权限的API调用（返回403错误）

---

## 📊 权限配置示例

### 示例 1：标注员

**需求**：只能标注任务和查看个人绩效

**配置**：

```json
{
  "role": "annotator",
  "name": "标注员",
  "permissions": [
    "Dashboard",
    "Console",
    "Task",
    "TaskPool",
    "MyWorkspace",
    "WorkLog",
    "WorkLogManagement",
    "Performance",
    "PersonalPerformance"
  ]
}
```

**效果**：

- ✅ 可以看到：仪表板、标注任务、工作日志、个人绩效
- ❌ 看不到：项目管理、任务审核、团队绩效、系统管理

---

### 示例 2：审核员

**需求**：标注员权限 + 审核任务 + 查看团队绩效

**配置**：

```json
{
  "role": "reviewer",
  "name": "审核员",
  "permissions": [
    "Dashboard",
    "Console",
    "Task",
    "TaskPool",
    "MyWorkspace",
    "TaskReview",
    "WorkLog",
    "WorkLogManagement",
    "Performance",
    "PersonalPerformance",
    "TeamPerformance"
  ]
}
```

**效果**：

- ✅ 标注员的所有权限
- ✅ 额外权限：任务审核、团队绩效

---

### 示例 3：项目经理

**需求**：审核员权限 + 项目管理 + 知识管理

**配置**：

```json
{
  "role": "project_manager",
  "name": "项目经理",
  "permissions": [
    "Dashboard",
    "Console",
    "Project",
    "ProjectDashboard",
    "ProjectManagement",
    "Task",
    "TaskPool",
    "MyWorkspace",
    "TaskReview",
    "WorkLog",
    "WorkLogManagement",
    "Articles",
    "MeetingNotes",
    "ModelTests",
    "CollaborationManagement",
    "Performance",
    "PersonalPerformance",
    "TeamPerformance"
  ]
}
```

**效果**：

- ✅ 审核员的所有权限
- ✅ 额外权限：项目管理、知识与文章

---

## 🔍 权限验证机制

### 1. **菜单过滤**

**后端实现**：`backend/app/api/menu.py`

```python
# 1. 获取用户角色
role = db.query(Role).filter(Role.role == current_user.role).first()

# 2. 解析权限列表
permissions = json.loads(role.permissions)  # ["Dashboard", "TaskPool", ...]

# 3. 递归过滤菜单
def filter_menus(items):
    filtered = []
    for item in items:
        if item["name"] in permissions:
            filtered.append(item)
        elif item.get("children"):
            kept_children = filter_menus(item["children"])
            if kept_children:
                item["children"] = kept_children
                filtered.append(item)
    return filtered

filtered_menu = filter_menus(menu_list)
```

### 2. **API权限验证**

**实现**：`backend/app/utils/permissions.py`

```python
@router.delete("/users/{user_id}")
def delete_user(
    user_id: str,
    current_user = Depends(require_permission("UserManagement"))
):
    # 自动验证用户是否拥有 UserManagement 权限
    # 无权限时返回 403 Forbidden
    pass
```

### 3. **前端按钮控制**

**实现**：`src/store/modules/user.ts`

```vue
<template>
  <el-button v-if="userStore.hasPermission('WorkLogManagement')" @click="manageWorkLog">
    管理工作日志
  </el-button>
</template>

<script setup>
  import { useUserStore } from '@/store/modules/user'
  const userStore = useUserStore()
</script>
```

---

## ⚙️ 技术细节

### 数据库存储

**roles 表**：

```sql
CREATE TABLE roles (
    id VARCHAR(36) PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    role VARCHAR(50) NOT NULL,
    description TEXT,
    is_active BOOLEAN DEFAULT TRUE,
    permissions TEXT,  -- JSON数组字符串
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

**permissions 字段示例**：

```json
["Dashboard", "Console", "Project", "TaskPool", "MyWorkspace"]
```

### API接口

| 接口                           | 方法   | 说明         |
| ------------------------------ | ------ | ------------ |
| `/roles/`                      | GET    | 获取角色列表 |
| `/roles/{role_id}`             | GET    | 获取角色详情 |
| `/roles/`                      | POST   | 创建角色     |
| `/roles/{role_id}`             | PUT    | 更新角色     |
| `/roles/{role_id}`             | DELETE | 删除角色     |
| `/roles/{role_id}/permissions` | GET    | 获取角色权限 |
| `/roles/{role_id}/permissions` | PUT    | 更新角色权限 |

---

## 🐛 常见问题

### Q1: 修改权限后为什么没有生效？

**A**: 用户需要**重新登录**才能加载新的权限配置。

**原因**：

- 菜单在登录时加载
- 权限配置存储在 JWT Token 中

**解决方法**：

1. 通知用户重新登录
2. 或者实现前端权限实时刷新机制

---

### Q2: 管理员能看到所有菜单吗？

**A**: 是的，`admin`、`super`、`administrator` 角色无需配置权限，自动拥有所有权限。

**代码实现**：

```python
if current_user.role.lower() in ['admin', 'super', 'administrator']:
    # 管理员返回全部菜单，不过滤
    return {"menuList": menu_list}
```

---

### Q3: 如何防止用户直接访问URL绕过权限？

**A**: 必须在**后端API**做权限验证，前端权限控制只是UI层面。

**最佳实践**：

```python
# ❌ 错误：只在前端隐藏按钮
<el-button v-if="userStore.hasPermission('UserManagement')">
  删除用户
</el-button>

# ✅ 正确：后端必须验证权限
@router.delete("/users/{user_id}")
def delete_user(
    current_user = Depends(require_permission("UserManagement"))
):
    # 即使前端绕过，后端也会拒绝
    pass
```

---

### Q4: 权限树中看不到最新菜单怎么办？

**A**: 权限树的菜单来自后端 `/menu/list` 接口，确保：

1. 后端菜单配置正确（`backend/app/api/menu.py`）
2. 前端已刷新页面
3. 用户已重新登录

**调试方法**：

```bash
# 1. 查看后端返回的菜单结构
curl -X GET "http://localhost:8000/menu/list" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 2. 检查浏览器控制台
# 打开角色管理页面，查看 console.log 输出的菜单数据
```

---

### Q5: 能否给某个用户单独配置权限？

**A**: 当前系统是基于**角色**的权限控制，不支持直接给用户配置权限。

**解决方案**：

1. 为该用户创建专属角色
2. 配置该角色的权限
3. 将用户分配到该角色

**示例**：

```
用户：张三（特殊权限需求）
  ↓ 创建专属角色
角色：张三_专属（data_analyst_zhangsan）
  ↓ 配置权限
权限：["Dashboard", "Project", "Performance"]
```

---

## 📝 权限矩阵（参考）

| 功能模块       | 管理员 | 项目经理 | 审核员 | 标注员 |
| -------------- | ------ | -------- | ------ | ------ |
| **仪表板**     |
| 控制台         | ✅     | ✅       | ✅     | ✅     |
| **项目管理**   |
| 项目仪表板     | ✅     | ✅       | ❌     | ❌     |
| 项目列表       | ✅     | ✅       | ❌     | ❌     |
| **标注任务**   |
| 任务池         | ✅     | ✅       | ✅     | ✅     |
| 我的工作台     | ✅     | ✅       | ✅     | ✅     |
| 任务审核       | ✅     | ✅       | ✅     | ❌     |
| **工作日志**   |
| 周列表         | ✅     | ✅       | ✅     | ✅     |
| **知识与文章** |
| 会议记录       | ✅     | ✅       | ❌     | ❌     |
| 模型测试       | ✅     | ✅       | ❌     | ❌     |
| 团队协作       | ✅     | ✅       | ❌     | ❌     |
| **标注绩效**   |
| 个人绩效       | ✅     | ✅       | ✅     | ✅     |
| 团队绩效       | ✅     | ✅       | ✅     | ❌     |
| **系统管理**   |
| 用户管理       | ✅     | ❌       | ❌     | ❌     |
| 角色管理       | ✅     | ❌       | ❌     | ❌     |

---

## 🎯 最佳实践

### 1. **角色命名规范**

| 建议             | 说明                                 |
| ---------------- | ------------------------------------ |
| **role（编码）** | 全小写+下划线，如：`project_manager` |
| **name（名称）** | 中文显示名，如：`项目经理`           |
| **description**  | 详细说明角色职责和权限范围           |

### 2. **权限分组策略**

**按业务模块分组**：

- 标注相关：`TaskPool`, `MyWorkspace`, `WorkLog`
- 审核相关：`TaskReview`, `TeamPerformance`
- 管理相关：`Project`, `UserManagement`, `RoleManagement`

### 3. **最小权限原则**

只授予用户完成工作所需的**最小权限集合**。

**示例**：

- ❌ 错误：给标注员 `UserManagement` 权限
- ✅ 正确：给标注员 `TaskPool` + `MyWorkspace` 权限

### 4. **定期审查**

建议每季度审查用户权限：

1. 检查是否有权限过度授予
2. 移除离职用户的角色
3. 根据业务变化调整权限

---

## 📚 相关文档

- **权限管理体系**: `docs/PERMISSION_MANAGEMENT.md`
- **菜单重组说明**: `docs/MENU_REORGANIZATION.md`
- **后端菜单配置**: `backend/app/api/menu.py`
- **权限验证工具**: `backend/app/utils/permissions.py`
- **角色管理API**: `backend/app/api/roles.py`
- **前端角色管理**: `src/views/system/role/index.vue`

---

## 🔧 开发指南

### 添加新权限

**步骤**：

1. **后端添加菜单项** (`backend/app/api/menu.py`)

   ```python
   {
       "path": "new-feature",
       "name": "NewFeature",  # ← 这就是权限标识符
       "component": "/feature/index",
       "meta": {"title": "新功能", "keepAlive": True}
   }
   ```

2. **API使用权限验证** (`backend/app/api/feature.py`)

   ```python
   @router.get("/feature/")
   def get_feature(
       current_user = Depends(require_permission("NewFeature"))
   ):
       pass
   ```

3. **配置角色权限**

   - 进入角色管理
   - 在权限树中勾选 "新功能"
   - 保存

4. **测试**
   - 用户重新登录
   - 验证菜单显示
   - 验证API权限

---

**最后更新**: 2025-10-16  
**作者**: AI Assistant  
**版本**: v1.0（角色权限管理使用指南）
